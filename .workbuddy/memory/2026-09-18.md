# 2026-09-18 工作日志

## 问答：3 秒 / 4 秒的镜头是否也得按 15 秒生成

**结论：不用，理论上就该按真实时长生成。** 现在一律跑 362 帧满窗是模板把参数写死的配置问题，不是设计。

- 依据：node 6 `MiniMaxH3AudioConditioningT8` 的 `length` —— default **124**（5.17 s）、min 5、step 17，
  tooltip 原文 `"24fps; snapped up to the 17n+5 H3 grid"` → 短时长本就是节点原生一等公民。
- 换算（24 fps，栅格 17n+5）：3.0 s = 72 帧 → snap **73**；4.0 s = 96 帧 → **107**（上取 4.46 s）或 **90**（下取 3.75 s）。
- 成本（A100 热态，实测拟合 草稿 `0.0243·n^1.362` + 精修 `0.050·n^1.243`）：
  - 3.0 s 镜 = 8.4 + 10.4 = **19 s** vs 满窗 149 s → **快 8.0×**
  - 4.5 s 镜 = 14.1 + 16.6 = **31 s** → **快 4.8×**
- ★ 提醒口径：按 3 秒生成 ≠ 只花 3 秒（成本 ≈ N^1.36，非线性）。
- 与 v2 审查（Mode B）同一件事：窗口覆盖率 3.69× → 1.09×，两片 9.52 h → 2.35–4.46 h。
- 前提未变：短窗**未实测**，验证四问（短窗出片 / 画质同档 / 音轨对齐 / LTX trim 到 89 帧影响字幕轴）。

## 审核：《现代婚姻三部曲》24 镜投产前审查（story/待生成_现代婚姻三部曲）

**判定红灯：不能按现状投产。** 交付 `00_投产前审核结论_20260918.html`（浅色报告）。

- 范围：3 部 × 8 镜 × 15 s（362 帧满窗，覆盖率 1.005×，这里 length 设对了不是 Mode A）。
  seed 20260918 / 20260920 / 20260922，16:9 横版，S1 672×384 → S2 1344×768。
- ★ **画面文字诱发项 12/24 镜（50%）**，4 镜要求"推近读字"：片1镜1 胸花 `reading Groom`、片1镜2 纸条 38万8、
  片1镜4 存折、片2镜2 毛笔字契约、片2镜5 银行凭证、片2镜6 双喜剪纸、片3镜1 房产证+协议、片3镜2 签名虚线、
  片3镜4 手写借据、**片3镜5 微距特写 `1,200,000 RMB` + 五个红手印（最贵最必废）**、片3镜7/8 文件与霓虹。
  且每镜结尾又写 `no on-screen text` → **指令自相矛盾**，产出伪字比纯乱码更难看。
- ★ **身份锚点归零 6 镜次**，4 个是多人同框：片1镜8（S1+S2 全 0）、片2镜6、片2镜8、片3镜6（三人同框全简写）、
  片2镜7（仅 hair）、片3镜4/6/7/8 男主只有 "spectacle"。片3男主 6 镜次从未给姓名+五官。
- ★ **identity 补丁白做**：`manifest.identity_locked.json` 已生成，但 `10_run_film.py` L48 读的是 `manifest.json`；
  且补丁本身有 4 处拼接 bug（`rosessweat` / `trousers[00:00` / `suit<Subject 2>` / 缺逗号）。
- ★★ **换脸机理**：Stage2 `#10` 走 `MiniMaxH3SolEngineLTXRefinerSetupT8Advanced` = 官方 Sigma **0.909375 起步**
  （91% 噪声重画）。服务器上已有官方保脸版 `<code>2026-08-30_..._Identity_Preserve_3Step_Advanced_EXP.json</code>`
  （0.5 起步），节点类 `MiniMaxH3SolEngineLTXIdentityRefinerSetupT8Advanced` 已注册，换掉零成本。
- ★ **A100（kehu 49.0.196.218，A100-PCIE-40GB SM8.0）与工程对不上 4 处致命**：
  ① `/workspace/h3scripts/80_run_workflow_remote.py` **全盘不存在** → 第一镜就崩；
  ② CLIP 写 `nvfp4_awq`（Blackwell 原生，A100 SM8.0 不支持）→ 须换 `qwen3vl_32b_minimax_h3_int8_convrot`（27.14 GB，已有）；
  ③ Stage2 UNET 写 `dev-transformer` + LoRA → 须换融合版 `ltx-2.5-22b-distilled-transformer-comfy-int8-convrot` 并摘掉 LoraLoader（= `s2_adapt.py` 干的事）；
  ④ ComfyUI pid 88272 常驻占 **29.5/40 GB**（已跑 12 h）→ 必须重启干净起跑。
- 隐患：`#3` 的 `target_width/height` **不是 widget input**（inputs 只有 frames/fps）→ `--set` 大概率无效 → 实际按写死的 1920×1088 跑。
  `#15 RandomNoise seed=42` 全片固定 → 构图单向漂移。
- 逻辑硬伤 6 处：片1镜3「不下轿」vs 新娘已在婚房／片1镜2「大舅哥」无此人／片2镜2「三十万」来路+「小勇」无交代／
  片2镜6 算盘（镜3已崩珠，此处完好）／片3镜8「终于停不下来了」病句／片2镜4 身份掉档。
- **服务器上正解齐全**：`s2_adapt.py`（保脸预设）、`check_no_text.py`/`scan_text_band.py`（禁字）、`make_subs.py`（外挂字幕）、
  `11_run_voice_film.py`（**中文干声锁音轨** —— 明确注明 `10_run_film.py` 的 T2VA 让 H3 自己说中文"台词不稳"）。
  即：是"翻译工程"不是"重建"。
- 成本：A100 热态 362 帧 ≈ 149 s/镜 → 24 镜两阶段批处理 68 min；identity（dense_reference）+ 1920×1088 实际 1.4–1.6 h。

## 逐镜重定时（三部曲 24 镜，15s 固定 → 11.375–15.042s 弹性）

- **病根**：不是 15s 太长，是"每镜都写死 15s"。H3 机制 = 给时间窗 + 三段 prompt 时间戳；
  三段写完窗口还剩时间，模型**不会停**，会无指令续演 → 各镜各编各的 = 强制割裂。
  **判定三标尺**：拍点数 / 台词量（5 字每秒）/ 主体是否运动（纯静态单主体撑不满 15s）。
  24 镜里只有 **6 镜**真撑得住满窗。
- ★★ **双重栅格（这次新算平的，比 length 那节更靠前）**：
  ① H3 `length` ∈ **17n+5**；② LTX `frame_policy` 默认 **`trim_to_8n_plus_1`**
  （已在 A100 `/object_info` 核实：*"Official route trims without adding frames"*）→ **只裁不补，向下取 8m+1**。
  10–15s 区间只剩 **8 个合法档**：226/243/260/277/294/311/328/345/362（H3 帧）→
  成片 9.375/10.042/10.708/**11.375/12.042/12.708/13.375/14.375★/15.042** s。
  ★ **345 帧（14.375s）是唯一双对齐无损档**（H3 出 345，LTX 裁完仍 345，一帧不丢）；
  其余各档被裁 1–7 帧 = 0.042–0.292s，剪辑无感但**字幕轴必须按裁后算**。
  本次**实际地板取 11.375s**（未启用 243/10.042）——地板两镜（片3镜1 环境 / 镜5 微距）都是三拍点，10s 偏赶。
- **落地**：`<story>/_后期/retime_shots.py`（可复现 + `audit()` 硬校验，不合规 **exit 2 一个字不写**）。
  `PLAN` 表逐镜写死档位，禁止填任意秒数（填 13.0 → 吸附 328 → 裁 321 = 实际 13.375s）。
  校验四项：h3_length ∈ 17n+5 ／ duration_frames == trim(h3_length) ／ 成片 ∈ [10.0,15.05] ／
  三段时间戳严格递增且末段终点 == H3 全长。
- ★ **两个必须同步改的下游**（只改 manifest 不生效）：
  ① 三个 `10_run_film.py` 里 `H3_LENGTH=362` 是**硬编码常量**，用于 `hdr` + `("6.length", H3_LENGTH)`；
  已改为 `L = int(shot.get("h3_length", H3_LENGTH))`，汇总行改显示区间。语法三份均通过。
  ② `_后期/make_subs.py` 兜底 `a.uniform or 15.04` → 改读 manifest 逐镜 `duration`；
  权威口径仍是 **ffprobe 实测累加**（Σ前序镜实测 + 镜内窗），兜底只在无底片时生效，误差不跨镜累积。
- **收益**：H3 总帧 8688→7770（−10.6%）、成片 361.0→320.0s、机时 60.0→52.0 min（−13.4%）。
  省机时是副产品，**真正收益是 18 镜不再有模型自己编的尾巴**。
- **踩坑**：`ts()` 一开始输出 `%.3f`（`5.042`）而非管线格式 `MM:SS.mmm`（`00:05.042`），
  把 24 镜 prompt 时间戳写坏 → 靠 `*.orig_retime` 备份还原重跑。**改 prompt 前先验格式**。
  另：中文路径下 Bash `grep` 静默失效（返回空），须用 Grep 工具。
- 产出报告：`待生成_现代婚姻三部曲/01_重定时结论_20260918.html`（浅色）。

## 记忆维护

- `MEMORY.md` 从 18 341 B 精简重写至 9 297 B（-49%）：删去与 `SKILL.md`、
  `分镜v2审查_对比v1.html` 重复的细节，改为「结论 + 指针」结构。
  最高价值新增/保留：`length` 参数与 Mode A/B 整节、窗口覆盖率判据、三口径计时表 + 3.7 倍定稿。

## 投产方案（待审，2026-09-18 09:11）—— 三点要求

用户明确三点：① 流水线两阶段批（stage1 全跑完 → stage2 加载参数精修），**写死**；
② 绝不能出现字幕，**必须在 prompt 里强制要求**；③ 按新分镜重审 manifest，老的归档，输出目录。
产出 `待生成_现代婚姻三部曲/00_方案_待审_20260918.html`，**未改任何工程文件**，等他确认。

- ★ **更正两处上轮结论**（都是我自己数错了，别再引用）：
  ① 画面文字诱发项是 **7 镜不是 12** —— 上轮把 document/paper/certificate 也算进去了，
     那些只描述物件不要求可读。真诱发 = 引号内文本 / `reading X` / handwritten / printed / calligraphy。
     清单：片1镜1(`reading Groom`) 镜2(`reading thirty-eight thousand eight hundred`) ／
     片2镜2(`calligraphy reading twenty thousand`) ／片3镜1·2(printed agreement) 镜4(handwritten notices)
     镜5(`"1,200,000 RMB"`+五手印，**最重**)。
  ② 禁字短语 **24 镜全都有** `no subtitles, no captions, no on-screen text` —— 不是缺，
     是**夹在第一段描述末尾当形容词**，不是独立强制段。→ 处置 = 拆互斥（7 镜改写）+ 加独立段
     `strict_output_constraints:`，单点定义在 `_pipeline/no_text_clause.txt` 由脚本注入。

- ★ **流水线现状（用户第①点的病根）**：`main()` 是 `for s in sel` 单循环，每镜
  `stage1_draft`→`stage2_refine` **交替**；默认 `--stage both` 就是逐镜交替。
  实测 批处理 149 s/镜 vs 逐镜交替 248 s/镜（差 66%，24 镜多付 ~40 min 纯搬运）。
  → 改：删掉 `both`，只留 all/draft/refine；两阶段之间**强制** `POST /free`；
  阶段门落盘 `_pipeline/gates/<片>.stage_gate.json` 支持断点续跑。

- ★★ **执行器状况（绕不开的前置）**：`/workspace/h3scripts/80_run_workflow_remote.py`
  **全盘不存在** → 第一镜就崩。但 **`/root/ui_to_api.py` 在**，且自带 `--submit --timeout`
  （UI→API 转换 + 提交 + 轮询），**只缺 `--set`**。两条路：
  A 补 `--set`（快，但踩 `SaveVideo.format/codec`=COMFY_DYNAMICCOMBO_V3 白名单坑，
  参数连键带值消失、跑完才报 missing 'format'）／**B 逐镜生成完整 API JSON（推荐）**，
  绕开全部 --set 坑且可存档 diff。B 顺带接 `s2_adapt.py --preset identity`。

- **manifest 三合一**：一个片目录躺着 4 份（manifest.json / identity_locked / *.orig_retime /
  manifest.orig_20260915.json）。★ `identity_locked` **从未被加载**（驱动第 48 行读 manifest.json）
  → 身份补丁白做。方案：合并成唯一 `manifest.json`（重定时 + identity + 禁字），
  删掉 identity_locked 中间态，老的进 `_archive/2026-09-15_首轮产物/`
  （含 `成片/` 147 MB、`_旧版对照_TAEHVv1/`、8 份旧报告）。

### A/B 选型结论（09:18，用户要求"扫描已生成的两个视频，学它"）

- **我上轮判断错了**：把 `80_run_workflow_remote.py` 当唯一执行器，它 A100 上不存在 → 以为要重建。
  错。那两片**根本没用它**，用 `/root/s2/submit_api.py`（A100 上现成，已产出 51 个 mp4）。
- **两个视频定位**：`h3-films/_deliver/v3/成片_film1.mp4`(223MB,08:14) / `成片_film2.mp4`(256MB,08:16)。
  铁证 `deliver_v3.sh:6` → `SRV="kehu:/workspace/ComfyUI/output/MiniMaxH3/v3run"` = **就在 A100 跑的**。
- **结论 B，不用选**：submit_api.py 吃 API JSON、无 `--set`。链路
  `build_workflows_v3.py --one <key>`（参数直写 JSON）→ `wf_<key>.json`
  （模板 `_remote/v2prev_tpl/s16.api.json` / `pg_B.api.json`）→ `run_v3.sh` 循环 submit_api.py。
  断点续跑 = keys.txt 删已成功 key。
- **用户第①点已被 v3 实现**：`run_v3.sh` 注释「模型只装两次，**绝不交替**」；keys.txt 前 34 T2V / 后 13 I2V。
- ★ **不能照抄的是工作流本身**：v3 = **一步直出 768×1344**（只 t2va/ref2va，无 Stage2），
  三部曲要两阶段。→ 照抄提交机制+批处理，换模板。两阶段模板链路已 A100 实跑通
  （s2_adapt --preset identity → ui_to_api → 22 节点 API）。
- **额外收获**：两阶段比一步直出**快 3.7 倍**（0.96h vs 3.56h）→ 坚持两阶段也是成本选择。
  v3 走**原声锁**（`wf["13"]["inputs"]["audio"]`），中文外部 TTS 干声，因 H3 自说中文"台词不稳"。
- 产出 `h3-films/00_AB选型结论_20260918.html`。待拍板：① B 确认 ② 台词走不走原声锁 ③ 7 镜文字道具保不保。

### 本轮执行（10:12–10:30，用户拍板 B + 原声锁 + 文字道具不保证）

- **★ 用户新给的 SOP（写进 `待生成_现代婚姻三部曲/AGENTS.md`）**：
  ① 开拍前 剧本→分镜确认表→他确认→才生成 manifest；
  ② 烧卡前 挑 2–3 个代表性镜头试拍（对话镜/产品镜/情感转折镜）→看成片 OK 才跑全量；
  ③ 出片后 视频+manifest→复盘报告（哪镜过、哪镜什么病、**归因到哪句提示词**）→只重生成失败镜；
  ④ **每轮复盘教训写回 AGENTS.md**（目标：第三轮后问题在校验阶段就被拦住，烧不到卡）。

- **已落地**：
  · `_pipeline/patch_notext.py` —— 7 镜文字诱发项降级为"不可辨"（8 处替换）+ 24 镜注入
    `strict_output_constraints:` 独立强制段（幂等）。老的归档 `_archive/2026-09-18_pre_notext/`，
    删掉从未被加载的 `manifest.identity_locked.json` 与 `*.orig_retime`。残留检查干净。
  · `_pipeline/tts_dry.py` —— 24 条 edge-tts 干声（24kHz mono，pad 到镜长），3 条零台词出静音。
    音色按角色分配。★ **片1 音色初版全配女声是错的**（镜2 新郎/镜3 丈母娘/镜4 父亲），已按镜重生成。
  · `_pipeline/build_stage1.py` —— 照抄 v3，参数直写 JSON（无 --set），24 份 API 工作流。
  · `_pipeline/run_server.sh` —— 两段流水线写死：阶段A全跑完 → `POST /free` → 阶段B（identity）。
  · `_pipeline/go.sh` —— 本地一键：上传 → nohup 启动。`pilot` / `full` / `status` 三模式。
  · `AGENTS.md`（规则库）+ `01_分镜确认表_20260918.html`（SOP ①）。

- **★ 试拍 3 镜挑定（原则：挑最难的，不是最好看的）**：
  `f1s02` 道具/产品镜（推近读纸条，禁字最难）／`f2s05` 情感转折（"老子不娶了"）／
  `f3s06` 对话镜·三人同框（一致性最难）。每片一镜。

- **★★ 服务器 kehu(49.0.196.218) 于 10:17 起全端口不可达**：ping 通、TCP 22/8188/… 全拒，
  累计重试 20+ 次。**疑似 OOM 后被隔离/重启**（10:12 时 `ram_free` 仅 3.6 GB / 67 GB）。
  → 试拍未跑成。用户带电脑出门，回来连网后 `bash _pipeline/go.sh pilot` 即可（约 8 分钟出 3 镜）。

- **★★ 原声锁的真实分叉（我此前理解有偏差）**：v3 manifest 明写
  `banned: "TTS / 任何生成声音（edge-tts 干声方案已废弃）"`，v3 的"原声锁"用的是
  **`orig_audio/`（从原片提取的真人声）**，不是 TTS。**三部曲是原创剧本没有原片**，
  所以只能在 edge-tts 干声 vs H3 自配音之间选，本轮按用户拍板走 TTS，效果待试拍验证。

- **待办**：服务器恢复后跑 pilot → 出复盘报告 → 修 → 才跑 full；
  角色定妆照常驻 `ref_images`（当前空 = 24 次独立摇号）；6 处逻辑硬伤未改。

### 剧本逻辑审查（10:45，用户要求"审查剧本和分镜，场景设定一致、符合逻辑"）

- 审查对象：3 份 `生成脚本_*.md`（**这才是原始剧本**，之前一直在审 manifest，
  manifest 只是它的派生）+ 24 镜。产出 `02_剧本逻辑审查_20260918.html`。
- **结果：P0 5 / P1 9 / P2 2，建议 P0 改完再开拍。**
- ★★ **P0 五处**（都是"观众会当场出戏"级，不是审美问题）：
  1. **片1 镜6→8 承诺与兑现不符** —— 台词"我带你去看真正的**海**"，结局是**雪山高原**，
     且剧本主题栏自己写着"自驾川藏线"（到不了海）。★ 这是最有杀伤力的一处，是情绪落点。
  2. **片2 镜2 "那三十万"全片从未交代** —— 观众无参照系，加价严重程度感知不到。
  3. **片2 镜2 "小勇"凭空出现** —— 全片无此角色，而它是加价 20 万的**唯一动机**。
  4. **片3 镜8 病句** "雨……终于停不下来了" —— "终于"(好事达成)+"停不下来"(坏事持续) 语义矛盾，
     且与 prompt 的 `profound liberation`、画面 rain-swept（雨还在下）**双冲突**。
  5. **片3 镜1→7 水桶物理不成立** —— 镜1 滴水，镜7 已满溢倾倒，剧情内只有几分钟。
- **P1 里明确不改的两条**（写下来防反复纠结）：片2 新娘缺席（纯雄性视角是风格）、
  片1/片2 结构雷同（先保单片成立）。
- ★ **沉淀的六维审查清单已写进 AGENTS.md 第九节**：承诺-兑现／前置交代／道具连续性／
  物理时间／用语与场景匹配／命名体系统一。下次审查直接照这六条过。
- **待用户拍板**：P0 是否现在改。改 3 处台词需重生成 TTS 干声（**要联网**，他出门前是窗口期）。

### v2 定稿 + 资产上服务器（11:00，用户"已联网，再次优化，给成品剧本，听我命令连服务器生成"）

- **服务器 11:00 恢复**：22 口开、ComfyUI 0.36.0 在线、ram_free 6G。
  ★ 但 **v3 的 `f2s03` 正在跑**（一步直出 768×1344，用 `orig_audio/film2/shot03.wav`，
  单进程 40042/40960 MiB）→ **未启动生成**，等它跑完。v3run 已完成 53 个 mp4。
- **剧本 v2 定稿已落地**：`_pipeline/patch_final.py` 改 台词 5 处 + 描述 7 处，
  老的归档 `_archive/2026-09-18_pre_final/`。产出 `03_成品剧本_定稿_v2_20260918.md/.html`
  （人物表 + 24 镜分镜表 + 修改记录 + 明确不改的三条）。
- **重出 5 条干声**：f1s03(Xiaoyi) / f1s06(Xiaoxiao) / f2s02(Yunjian) / f2s06(Yunjian) / f3s08(Yunxi)，
  时长与镜长对齐。24 条齐。
- **重建 24 份工作流 + prompts**（prompt 变了必须重建，否则白改）。
- **投产前八项校验 24 镜全过**：栅格/裁后帧/时长带/禁字段/时间戳/文字残留/干声对齐/工作流同步。
  ★ 校验脚本自身两坑：duration 要 0.001 容差；时间戳 `MM:SS.mmm` 必须带毫秒（否则 15.083→15.000 误报）。
- **资产已上传服务器**（只上传、未启动）：wf 24 / prompts 24 / 干声 24 / run_server.sh / keys.txt(pilot 3 镜)。
  目录 `/workspace/trilogy/`，干声 `/workspace/ComfyUI/input/tts_dry/trilogy/`。
- ★★ **实测校准（权威，从 history 反推）**：v3run 一步直出 768×1344 = **平均 406 s/镜**
  （328/341/341/388/448/501）→ 两阶段 ≈ **150 s/镜** → **24 镜 ≈ 60 分钟**。
  与既有账本（149 s/镜、60 min）**独立吻合**。比值 **2.7×**（记忆里另记 3.7× 是不同口径：
  3.7 是"两步走 vs 全 768 一步到位"含冷启摊分；2.7 是纯热态单镜比值）。两个都留，别混用。
- **等用户命令**才启动：`bash _pipeline/go.sh pilot`（3 镜）→ 复盘 → `full`（24 镜）。

## 11:26 · 三部曲试拍点火（pilot f1s02/f2s05/f3s06），点火前抓到两个致命 bug

### bug 1 · stage2 草稿匹配不上（会让 stage2 全跳）
`run_server.sh` 里 `DRAFT=$(ls $OUT/${k}_*.mp4)`，k=`f1s02`（无下划线），
但 stage1 的 `filename_prefix` 是 `MiniMaxH3/trilogy/f1_s02`（**带下划线**）
→ 匹配为空 → 每镜都"缺草稿，跳过"，stage1 白烧。
修：`PREFIX_K="${k:0:2}_${k:2}"`（f1s02→f1_s02），并加无下划线兜底。

### ★★ bug 2 · s2_adapt 的「UI/API 格式」陷阱（静默毁结果，最难查）
`s2_adapt.py` 的 `adapt_template` 与 `parametrize` **全部按 API 格式**（顶层 key=节点 id）操作。
官方工作流 `2026-08-30_H3_Sol_Engine_LTX25_Identity_Preserve_3Step_Advanced_EXP.json` 是 **UI 格式**（含 nodes 数组）。
若直接把 UI 模板喂给 s2_adapt：
- 所有 `d["8"]/d["9"]/d["25"]` 取空 → **A/B/C 三处改造一条都不执行**
- 还打印「模板已是适配态，未做改动」**谎报成功**
- 随后 `parametrize` 找 `d["3"]` → `KeyError: '3'` 崩
- 若拿它产出的 UI json 再喂一次，同样崩（节点已被重排成 10 个）

**正确链路（顺序硬性）**：
```
官方UI --ui_to_api.py--> s2_raw.api.json
       --s2_adapt(A/B/C + --preset identity)--> s2_tpl.api.json
       --s2_adapt(--shot/--draft/--prompt-file/--seed)--> s2_<key>.api.json
       --submit_api.py--> ComfyUI
```
parametrize 依赖的节点 id（官方模板固定）：3=target_w/h、1=LoadVideo.file、
12=CLIPTextEncode.text、15=RandomNoise.noise_seed、20=SaveVideo.filename_prefix、8=UNETLoader。

**校验三处改造是否真的生效**（别信 stdout）：
`8.unet_name` 含 distilled、`"9" not in d`（LoRA 已删）、`17.class_type == VAEDecode`、`"25" not in d`。
已在 run_server.sh 里写死 assert 校验，不过就 exit 1。

### 其它
- stage2 seed 从 stage1 的 `wf json node9.noise_seed` 提取写入 `seeds.txt`，保证两阶段同源。
- 等待器 `wait_go.sh`：三重判空闲（v3 进程消失 + 队列 0 连续两次 + 显存<8G）→ free 显存 → 才起 run_server.sh。避免撞车 OOM。
- `pkill -f wait_go.sh` 后 `&&` 会因 rc 非 0 短路，需单独确认 pgrep。

## 12:04 · pilot 正式点火（前两次空转的教训）

### ★ 事故 1 · wait_go 显存判定死锁 30 分钟
等待器把「显存 < 8GB」写进空闲判定 → 从 11:26 干等到 11:56 都没启动。
原因：**ComfyUI 跑完任务后不会自动释放权重**，会常驻 20~29 GB（v3 结束后实测
28801 MiB，同时 GPU 利用率 0%）。**显存高 ≠ 在忙。**
正解：空闲判定只用「v3 进程消失 + 队列空（连续两次）」，判定通过后**主动 POST /free**。
实测 free 效果：显存 28801 → 675 MiB，内存可用 6.9 GB → 59.4 GB（放出 52 GB）。

### ★ 事故 2 · `pkill -f wait_go.sh` 会把自己也杀了
执行它的 ssh bash -c 命令行里同样含 "wait_go.sh" 字符串 → 自杀 → ssh 断连且**无输出**，
看起来像"命令静默失败"。排查时用精确匹配：
`ps -eo pid,args --no-headers | awk '$2=="bash" && $3=="wait_go.sh"'`（注意 heredoc/双引号里 $ 要转义）。
同类：pgrep -f 也会匹配到自己。

### 结论
- 服务器 11:41 那次"宕机"其实**没重启**（12:00 复查 uptime 19:29 未变），只是 sshd/网络层断连；
  ping 通 + 22 关闭 ≠ 实例崩溃，也可能是 sshd 被打爆（当时内存只剩 6.9 GB）。
- 真正的隐患是**内存**：ComfyUI RSS 长期 56 GB / 64 GB，available 一度只剩 4.5 GB。
  每次点火前先 POST /free（连 RAM 一起放），不要叠加任务。

## 12:35 · 资产迁仓 + 切换一步直出 + manifest 重出

### 目录迁移
`~/Desktop/story/待生成_现代婚姻三部曲` → **`~/Desktop/videoHub/待生成_现代婚姻三部曲`**，
并入 videoHub git 仓库。story 仓库那 166 个无关变更**未动**（用户说"历史的先不用管"）。
- videoHub 根 `.gitignore` 已统一排除 mp4/wav/png/jpg/safetensors 等大媒体，
  **不要再建子级 .gitignore**（我建了又删，冗余）。
- 提交：`28dfce2`（接入资产 173 文件）`197961c`（一步口径 manifest 33 文件）

### ★★ 画质决策：两阶段 → 一步直出 768×1344
试拍反馈精修版糊。机理：stage2 从 384×672 草稿**上采样补细节**（identity 预设
只从 sigma 0.5 起步，保留一半草稿 latent），不是原生 768 生成。

**关键认知（逐节点比对确认）**：v3run 与本流水线 stage1 是**同一个工作流** ——
同 UNET `minimax_h3_fl2va_pruned_int8_convrot`、同 4 步 `dual_clock_euler`/`native_flow`、
同 15 节点、**无图片输入节点**（fl2va 走纯文本路径）。唯一差别是分辨率与音轨。
→ "一次生成"不用换流程，只改分辨率 + 跳过 stage2。

**v3 实测基准**（run_v3.log 47 样本，权威）：平均 **466 s/镜**（中位 450，范围 332–651）
@ 平均 297 帧。我们 24 镜平均 324 帧（长 9%），按幂律 1.362 上修 → **508–525 s/镜**。
**24 镜 ≈ 3.4–3.6 h（约 9 元）**；两阶段 24 镜 ≈ 1.1 h。比值 **3.2×**。

### ★★★ manifest 口径（最易漏，已踩）
一步直出**不经 LTX trim**，成片 = H3 原始帧数。
`duration`/`duration_frames` 必须从 trim 值改回 H3 值：
f1s02 305帧/12.708s → **311帧/12.958s**（Δ+0.250s）
全片 24 镜 **320.00s → 323.75s（+3.75s）** → **字幕轴必须按新值重算**。
- prompt **不用改**（时间戳末段本就按 H3 全长）
- 干声 **不用重出**（tts_dry 本来就按 H3 时长）
- 脚本 `_pipeline/patch_manifest_onestep.py`，24 镜校验全过

### 校验脚本的两个坑（我自己写错两次）
1. key 格式是 `f1s01`（不是 `01s01`）—— 目录名 `01_周星驰...` 取首位数字才是片号。
2. 正则抓时间戳**不能加 `\[` 前缀**：`[00:05.042 – 00:10.042]` 每行两个时间戳，
   加 `\[` 只会抓到行首那个，末段取错 → 误报"prompt 未覆盖全长"。
   正确：`re.findall(r"(\d{2}):(\d{2})\.(\d{3})", prompt)` 取 `[-1]`。

## 13:18 · 一步直出试拍三镜完成（f1s02/f2s05/f3s06）

### 实测计时
| 镜 | 类型 | 用时 |
|---|---|---|
| f1s02 | 产品/道具 | **651 s / 10.85 min**（含冷启动） |
| f2s05 | 情绪 | **570 s / 9.50 min** |
| f3s06 | 三人对话 | **615 s / 10.25 min** |
| **合计** | | **1836 s / 30.6 min** |

24 镜估算：1 冷启 + 23 热态 = 651 + 23×585 ≈ **3.92 h**（±10% → **3.7–4.3 h**）。

### 画质结论
一步直出 768×1344 **明显更硬、更锐**（毛孔/发丝/纽扣/面料纹理都更清晰），
两阶段因为是从 384×672 草稿上采样补细节，有**涂抹感、油润感、动态拖影**。

保留问题（与分辨率无关）：
1. f1s02 门/纸条、背景对联等**场景道具文字**仍清晰可读
2. f3s06 仍然**多了一只手/半个第四人**（提示词构图问题）

报告：`_deliver/onestep_20260918/对比_一步直出vs两阶段_20260918.html`
成片：`_deliver/onestep_20260918/refined/`

### 24 镜全量下一步
- 清空 `gates_one/` 和远端 `trilogy_one/` 输出（保留试拍对照）
- 生成 `keys_full.txt` 跑 `run_one_step.sh`
- 出片后按 H3 duration 重算字幕轴
- 后期处理道具文字与 f3s06 多出的手

## 13:45 · "定心丸"字幕根因查明 + f3s06 修复验证

### 根因
**`<d>` 标签** 是 MiniMax H3 的 dialogue 结构化指令。21/24 镜 prompt 用了
`<d>[Chinese] "..."</d>`，模型在驱动口型的同时，把标签内的中文台词
**当作画面字幕渲染**。尾部自然语言 "no subtitles" 优先级盖不过结构化标签。

### 表情僵硬根因
24 镜 prompt 扫描：20 镜微表情 <3 处；充斥 `spine straight / unnervingly calm /
completely motionless / frozen in place` 等完全静态词；缺少 `jaw tighten /
blink / swallow / lip tremble / muscle twitch` 等微动作。

### f3s06 修复验证
- 去掉 `<d>`，改为 `delivers an icy verdict in Chinese`
- 增加新郎下颌收紧/冷笑一闪/缓慢呼吸，母亲眨眼/张口，新娘吞咽/嘴唇颤抖
- 修复版结果：屏幕字幕消失，人物更生动，道具文字更模糊，构图从"文件特写"转向"人物表情"

报告：`_deliver/onestep_20260918/对比_f3s06_修复版vs原版_20260918.html`
24镜审查：`_deliver/prompt_表情审查_20260918.md`

### 决策点
全量 24 镜需批量去掉 `<d>` + 增强微表情。是大工程，需用户确认后再执行。

## 15:10 · 定心丸根因复核通过 + f3s06 人物关系修正

### 复核结论
`<d>` 根因**成立**：`cmp_v2/f3s06_v1_2.0.jpg` 里烧着"是5二十万的替死鬼"
（`<d>` 台词"一百二十万"被模型画歪 → 佐证是模型自绘字幕）。修复版无字幕。

### 新发现 + 修复（fix_film3_staging.py）
1. **用户截图（f3s06_v2_12.5）人物关系错**：新郎与丈母娘并肩同侧、新娘消失。
   根因：a) "across the table + S1 foreground 脸朝镜头" 几何自相矛盾 → 对面两人
   被迫搬到家旁边；b) f3s06 唯一写 S3 为 "the 56-year-old mother"（未锚 mother-in-law）
   → 读成他的妈。
   修法：90° 剖面对峙构图 + mother-in-law/her daughter 锚定 + Exactly three people
   （顺带治第四只手）+ 半地下室环境锚。
2. **f3s02 的 `<d>` 就是"定心丸"台词** —— 片3 其余 4 镜（02/03/04/08）已全部去 `<d>`，
   片3 现 0/8；口型靠 `<Audio 1>` 干声，台词不丢。
3. **前导段口径**：manifest 不带 `<Audio 1>` 前导段，wf = 前导段 + manifest
   （前轮把全文写进 manifest，重跑 build_stage1 会双份）。8 镜校验 PASS。
4. 片1/片2 仍 16 镜带 `<d>`，批量处理待确认。

报告：`_deliver/审查_定心丸根因复核与f3s06人物关系_20260918.md`

## 14:40 · 剧本审查标准 v3 + 24 镜 prompt 全量优化

### 交付
**《剧本审查标准 v3》**（`05_剧本审查标准_v3_20260918.html`）四条硬标准：
- S1 零字幕：无 `<d>[Chinese]"…"</d>` + 有 strict_output_constraints + 文字道具带 illegible
- S2 表情到位：微表情 ≥3 · 情绪过渡 ≥1 · 完全静态词 =0
- S3 位置逻辑：多人镜方位锚点 ≥1 · 单人镜朝向锚点 ≥1 · 跨镜符合场景坐标表
- S4 技术对齐：prompt 末段 = duration · 工作流 768×1344 与 manifest 同源

配套**场景空间坐标表**（片1 楼梯间/闺房/车内；片2 堂屋/院门/挎斗；片3 餐桌），
规定每格场景里角色的固定方位，跨镜不得跳变。

### 执行
三轮优化脚本 `patch_prompts_v3.py` → `v3b` → `v3c`：
- 第一轮：去 `<d>`（16 镜）、补位置锚点、局部微表情
- 第二轮：每镜注入定制情绪收束句（≥3 微表情 + ≥1 过渡），23 镜
- 第三轮：补 5 处未达标（微表情计数/朝向）

**终审 24/24 全过**（`review_v3.py`）：平均微表情 4.8/镜、情绪过渡 2.3/镜，
残留 `<d>` = 0，残留完全静态词 = 0。

### 发现
- **片3 的 manifest 早已去 `<d>`**（v2 阶段改的，写成 `speaks ... in Chinese, <表演提示>.`），
  但 `_pipeline/prompts/*.txt` 与 `_pipeline/wf/` 还留着旧的 `<d>` → 已全部同步。
- **替换文本必须以句号结尾**，否则会和后一句粘成 `eagerly Suddenly`
  （`</d> Suddenly` 这类拼接点）。已修 + 写进脚本。

## 15:00 · 硬标准升格为「所有剧本的通用规范」

用户：「然后更新对应 AGENT.md 以及记忆吧，以后的剧本都要这么要求」

### 落地三处

**① 项目 AGENTS.md — 新增 §零（升到最前）**
《剧本生产硬标准（所有剧本强制 · 不通过不得烧卡）》：
- 0.1 四条硬标准（S1 零字幕 / S2 表情到位 / S3 位置逻辑 / S4 技术对齐）+ 两条机理（`<d>` 标签
  是字幕直接诱因；H3 干声定口型、神态全靠 prompt）
- 0.2 **场景空间坐标表模板**（每格场景规定机位/角色方位/道具位）+ 三人同框硬要求
  （90° 剖面 + 屏幕方位 + 关系词锚定 + `Exactly three people`）
- 0.3 三关流程（自动扫描 → 试拍目视 → 复盘写回）
- 0.4 烧卡前检查清单（6 项）
- SOP 加了第 ⓪ 步「剧本定稿 → 建坐标表 → 过四条硬标准」
- 顺手修掉编号重复（三个「十、」→ 保留一个，另两个改「附录 A/B」；两个「十五、」→ 第二个改「十六」）

**② 项目记忆 MEMORY.md — 精简重写（14880 → 4991 字节，-66%）**
原因：超限被截断注入。改为「判据索引」式：只留跨会话必用的，
细节指向 `~/.workbuddy/skills/h3-film-production/SKILL.md`（973 行）与 AGENTS.md。
首条即「★★★ 剧本生产硬标准（所有剧本强制）」。

**③ 用户级 ~/.workbuddy/MEMORY.md — 新增跨项目条目**
在原「不烧字幕」条目后，加「剧本生产四条硬标准（2026-09-18 起强制，跨项目适用）」，
含四条标准摘要 + 三关流程 + 技能脚本路径。→ 以后**任何项目**里开新剧本都会带上。

### 技能侧（上一轮已做，本轮确认覆盖完整）
`~/.workbuddy/skills/h3-film-production/SKILL.md` 硬约束 4「剧本 Prompt 必须过四条硬标准审查」
+ `scripts/review_prompts.py`（通用扫描器）。本轮核对：章节、判定口径、三关流程均已覆盖。

### 引用关系（别搞混）
- **规范条文**（唯一权威）→ `待生成_现代婚姻三部曲/AGENTS.md` §零
- **技术细则** → `~/.workbuddy/skills/h3-film-production/SKILL.md`
- **跨项目触发** → `~/.workbuddy/MEMORY.md`（用户级）
- **扫描脚本** → 本项目 `_pipeline/review_v3.py`；通用 `~/.workbuddy/skills/.../scripts/review_prompts.py`

## 15:10 · f3s06 修复版重生成完成（一步直出）

- 14:52 提交 `wf_one/wf_f3s06.json`（剖面构图+关系锚定版），15:05 出片，
  含冷启 **780 s**。成片：`_deliver/onestep_20260918/regen_f3s06/f3s06_修复重生成.mp4`。
- 体检：362 帧/15.083s、768×1344、aac 2ch，与 manifest 一帧不差。
- **三个旧病全消**：无字幕、三人同框（母女相邻为一家单元、新郎独处近端）、
  无第四只手；表情有变化不再僵直。
- **残留**：H3 把"90° 剖面隔桌对峙"画成三人同排（都朝同侧），对峙感弱化但
  关系不再误读。竖幅三人正反打是 H3 弱项；要硬对峙只能换 seed 重试或改过肩双人镜。
- 服务器操作记录：`gates_one/f3s06.done` 备份为 `.pilot1_bak` 后重跑；
  keys.txt 暂改为单镜 `f3s06`（原 pilot 三镜备份在 `keys.txt.pilot_bak`）；
  显存先 /free（22433→675 MiB）再点火。

## 17:00–18:10 · 服务器重置后全量重建 + 双卡投产（kehu 49.0.196.218）

**背景**：系统被重装（/workspace、/root/s2、venv 全空），机器升级为
**2× A100-PCIE-40GB / 125 GB 内存 / 16 核 / 148 GB 盘**，机房在**华为泰国**。

### 机房网络实测（判据，别再试错）
| 源 | 速度 | 结论 |
|---|---|---|
| HuggingFace resolve（-L） | **35–42 MB/s** | ★ 权重唯一来源 |
| hf-mirror.com | 37 MB/s | 备用 |
| PyPI / download.pytorch.org | 39 MB/s | 正常 |
| GitHub | 2 MB/s | 正常 |
| **ModelScope** | **9 B/s** | ✗ 泰国机房等于不通，原映射表来源全部作废 |

★ 测速必须用 `curl -L`——不带 -L 量到的是 302 重定向页面（当时误读成 2.4 KB/s）。

### 重建步骤（已跑通）
1. 驱动：自带 470 且已加载 → `nvidia-uninstall --silent` + 停 `nvidia-persistenced`
   + `rmmod` → `sh NVIDIA-Linux-x86_64-580.95.05.run --silent --dkms`（校验 `--check` 先过）
2. venv：Ubuntu 22.04 的 python3.10 缺 ensurepip → 先 `apt install python3.10-venv`
3. torch：`--index-url https://download.pytorch.org/whl/cu130` 装
   `torch==2.14.0+cu130 torchvision==0.29.0+cu130 torchaudio==2.11.0+cu130`（CUDA 13.0 ✓ 2 GPU 可见）
4. ComfyUI：`git init` + `fetch --depth 1 origin f42b24ef` + `checkout FETCH_HEAD`（v0.35.0）
5. T8 节点：clone `T8mars/comfyui-minimax-h3-audio-T8` → checkout `e12d8af`（v1.79.6），无额外依赖
6. 权重 5 个 52 GB 全走 HF，24 分钟下完

### 三个新踩坑
- **权重路径带子目录**：工作流要 `text_encoders/minimax_h3/qwen3vl_32b_minimax_h3_int8_convrot.safetensors`，
  直接放 text_encoders 根 → 400 `value_not_in_list`。
- **必须装 ffmpeg**：`apt install ffmpeg`，否则 T8 保存节点报
  `FFmpeg is required for isolated H.264 long-video encoding`（生成跑完 590 s 才在最后一步炸）
- **shell 优先级坑**：`cd X && A & B &` 里 B 跑在原 cwd → 用绝对路径提交。

### 双卡方案（ComfyUI 不支持单实例跨卡）
两个进程各占一卡：`CUDA_VISIBLE_DEVICES=0 --port 8188` / `=1 --port 8189`，
24 镜拆 keys_a/keys_b 各 12 镜并行。单实例仅占 **45 GB 内存**，125 GB 内存扛得住两份。
★ 权重在本地盘 → **去掉 `--disable-mmap`**（云盘时代才需要）。
★ 每镜前仍 POST /free。

### 现状
- 试拍 f1s01 出片：768×1344 / 含音轨 / 15.084 s，与 manifest 15.083 一帧不差
- 18:08 双卡全量 24 镜已启动（`run_dual.sh a|b`，日志 run_a.log / run_b.log）
- 单镜实测定标：生成段 **590 s**（与旧机 570–651 s 一致）

### 18:15 用户叫停（试拍未通过目视前不烧卡）
- 按 PID 精确 kill（`pkill -f` 会自杀式断连）→ POST /interrupt 双端口 → POST /free
- 停下时状态：GPU 双卡 609 MiB 空闲、ComfyUI 两个实例仍活着（8188/8189 = 200）
- 已完成仅 `f1s01`；`f2s05` 跑到约 11 min 被中断，**无产出文件**（T8 保存节点在最后一步），
  gate 未打标记 → 恢复时会整镜重跑
- 恢复命令：`cd /workspace/trilogy && bash run_dual.sh a` 与 `bash run_dual.sh b`

## 18:20 · 服务器双卡就绪 + H3-LongVideos 节点包分析

### 服务器（升级核实）
2×A100-PCIE-40G（"128G"是系统内存 125G）。两 ComfyUI 0.35.0 实例常驻：
8188=CUDA0 / 8189=CUDA1（CUDA_VISIBLE_DEVICES 显式绑定）。
VAE 齐：video_fp16 + audio_fp32。custom_nodes 现有 comfyui-minimax-h3-audio-T8。

### Smite79/MiniMax-H3-Longvideos = ComfyUI 节点包（无权重！）
单 prompt → 自动拆镜链 → 拼整片（带同步音频）。四节点：H3-LongVideos /
Shot Length / Model Inspector / Overlay。要求 ComfyUI≥0.31 ✓。
**单镜上限 362 帧/15.08s 不变**——源码 H3_MAX_FRAMES=362、frames%17==5，
与我们实测的 17n+5 完全一致（交叉验证）。"长"靠拆镜链，不靠加长单镜。
亮点：角色表 + <Picture N> 参考图跟随人物 + 自动连续性条款 + exact:/verbatim 逃生口。

### 结论
- 加速：×2 可用。`run_one_step_dual.sh` 已写好并上传（双端口对半分发，
  空队列硬校验 + 各自 /free + gates 互斥），24 镜一步直出 3.9h→约 2h。
- 更好：参考图一致性需 hybrid fl2va/ref2va merge 权重（我们没有，
  要下载+重做 int8 才塞得进 40G）——治"24 次摇号"的根本路；纯 prompt 层
  的角色表/连续性条款今天就能搬进 manifest 生成器。
- 长度：不切整链。与原声锁（TTS 音色）/禁字条款/逐镜审查/不整片重跑冲突；
  吸收其法，保留逐镜之骨。

## 18:45 · ComfyUI v0.36.0 升级实测与 MiniMax-H3 加速验证（重大收益）

### 升级背景与动因
ComfyUI 官方于发布标签 `v0.36.0` 集中合入了针对 MiniMax-H3 的关键性能优化：
1. **PR #16187 (MiniMax-H3 VAE optimizations)**：
   - 3D 卷积融合：Norm / SiLU / Padding / Residual 全部并入 `comfy-kitchen` 专有算子 `ck.group_norm_silu_pad3d` 与 `ck.fp16_conv3d`。
   - Attention 与 FeedForward 激活与残差融合至 INT8 GEMM epilogue。
   - 融合 per-head `RMSNorm + RoPE`（`ck.rms_rope_split_half`）。
   - `tiled_decode` 重构为感知显存的动态并发批处理（单次解码最多 4 个 Tile，提升吞吐并降低碎片）。
2. **PR #16332 (Lower minimax VAE usage)**：优化时间分块缓存释放，降低长视频峰值显存。
3. **PR #16326 / #16351 (LLaMA RoPE fast kernels)**：加速 H3 文本编码器（Qwen/LLaMA 架构）Prompt Conditioning。
4. **PR #16329**：配套更新 `comfy-kitchen==0.2.34`。

### 升级实操（服务器 kehu）
1. 停旧实例：`kill -15 32988 36014` → `nvidia-smi` 显存归零（0 MiB / 0 MiB）。
2. 签出代码：`cd /workspace/ComfyUI && git checkout v0.36.0`（commit `ee71d5c4`）。
3. 依赖安装：`/workspace/venv/bin/pip install -r requirements.txt`，卸载 kitchen 0.2.33，成功安装 `comfy-kitchen==0.2.34` 及缺失的 `comfyui-workflow-templates==0.11.62`。
4. 验证算子：确认 `ck.group_norm_silu_pad3d` 与 `ck.rms_rope_split_half` 正常导出。
5. 双实例规范启动（修复 SQLite 并发锁）：
   - GPU 0：`CUDA_VISIBLE_DEVICES=0 nohup /workspace/venv/bin/python /workspace/ComfyUI/main.py --listen 127.0.0.1 --port 8188 --vram-headroom 1 > /workspace/logs/comfy_a.log 2>&1 &`
   - GPU 1：`CUDA_VISIBLE_DEVICES=1 nohup /workspace/venv/bin/python /workspace/ComfyUI/main.py --listen 127.0.0.1 --port 8189 --vram-headroom 1 --database-url sqlite:////workspace/instance_b.db > /workspace/logs/comfy_b.log 2>&1 &`
   - 双端口 8188 / 8189 均正常启动，零报错。

### 真实出片对比测试（wf_f1s02 试拍：768×1344，311 帧，13 秒）
| 指标 | 升级前（commit f42b24e / kitchen 0.2.33） | 升级后（v0.36.0 / kitchen 0.2.34） | 加速效果 |
| :--- | :--- | :--- | :--- |
| **模型初始化与加载** | 128.2 秒 | **96.8 秒** | ⚡ **快 31.4 秒**（提速 24.5%） |
| **单步扩散采样平均耗时** | 131.1 秒 / step | **94.3 秒 / step** | ⚡ **每步快 36.8 秒（提速 28.1%）** |
| **4 步扩散采样总耗时** | 527.0 秒 | **377.0 秒** | ⚡ **净省 150 秒（2分30秒）** |
| **单镜端到端总时长** | 590.7 秒（9分50秒） | **469.3 秒（7分49秒）** | 🚀 **净省 121.4 秒（提速 20.5%）** |
| **24 镜双实例全量排期** | 约 2.0 小时 | **约 1.56 小时（1h34m）** | 🚀 **整批净省 26 分钟** |

- 产物验证：`/workspace/ComfyUI/output/MiniMaxH3/trilogy_one/f1_s02_00001_.mp4`（18 MB，H.264+AAC，768×1344，311 帧，有效视频）。
- 门禁同步：已写回 `gates_one/f1s02.done`。

