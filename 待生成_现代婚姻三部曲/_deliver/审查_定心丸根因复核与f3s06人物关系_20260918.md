# 审查：定心丸字幕根因复核 + f3s06 人物关系修正（2026-09-18）

> 触发：用户要求审查"`<d>` 标签 → 定心丸字幕"根因结论，并指出 f3s06 修复版
> 截图（`cmp_v2/f3s06_v2_12.5.jpg`）里**新郎与丈母娘并肩同侧、新娘消失**，
> 人物关系不对，要求优化。

---

## 一、根因复核结论：`<d>` 标签字幕根因 **成立**，证据链完整

| 证据 | 内容 |
|---|---|
| 原版 v1 帧 `cmp_v2/f3s06_v1_2.0.jpg` | 画面下方烧着字幕 **"是5二十万的替死鬼"** —— 正是 `<d>` 台词"是一百二十万的替死鬼"的字幕化渲染（"一百二"被模型画成"5二"，佐证是模型自绘而非外挂字幕） |
| 修复版 v2 帧 `cmp_v2/f3s06_v2_2.0/7.5.jpg` | 无任何屏幕字幕 |
| 机制 | `<d>[Chinese] "…"</d>` 是 MiniMax H3 的 dialogue 结构化指令；训练数据里台词与屏幕字幕高度绑定，尾部自然语言 `no subtitles` 优先级盖不过结构化标签 |

**残留风险（本次审查新发现）**：
- 全 24 镜原有 21 镜带 `<d>`；f3s06 修复后仍剩 **20 镜**。其中 **f3s02 的 `<d>` 内容正是
  "女人要的是一份定心丸"** —— 不处理，"定心丸"字幕会在 f3s02 复发。
- **本次已把片3 全部 5 个带 `<d>` 的镜清零**（f3s02/03/04/08 去标签 + f3s06 前轮已去），
  口型由 `<Audio 1>`（tts_dry 干声）驱动，台词不丢。
- 片1/片2 仍有 **16 镜**带 `<d>`（f1：8/8，f2：8/8），批量清除待确认后执行。

## 二、f3s06 人物关系错误根因（用户截图）

画面事实（`f3s06_v2_12.5.jpg`）：新郎振浩与丈母娘**并肩同侧**而坐，像一对母子；
新娘恩熙整个消失（10.0s 帧只剩一只持笔的手）；背景变成暖色调餐厅（陈设盘、花瓶），
不是半地下室。根因两条，都在 prompt 里：

1. **构图几何自相矛盾**：原句 "Medium symmetrical shot **across the table**" +
   "S1 in the **foreground center**"（且描述了他镜片反光、眼神）——相机紧贴 S1 一侧；
   那 "**Across the table**, S3 on the left"（S1 的正对面）就在**镜头背后**，画面里根本
   放不下。模型只能就近解决：把 S3 拉到 S1 身边。
2. **关系词失锚**：f3s06 是片3 唯一把 S3 只写成 "**the 56-year-old mother**" 的镜
   （f3s02 写的是 "mother-in-law named MADAM-HE"）。一位"母亲"被放到新郎身边，
   模型自然读成**他的妈**，媳妇反而出局。

剧本本身的人物表（振浩 32 / 恩熙 28 / 何女士 56 丈母娘）没有错，**错在 f3s06 的
prompt 舞台调度**。

## 三、修复内容（`_pipeline/fix_film3_staging.py`）

f3s06 三段重写：
- **90° 剖面对峙构图**：新郎独坐近侧（右向侧面），丈母娘+新娘**肩并肩坐在远侧**、
  左向侧面面向他——双方脸都可见，对峙阵营一眼可读；
- **关系词锚定**：S3 = "his 56-year-old future **mother-in-law**"，S2 = "**her** 28-year-old
  **daughter** the bride"（母女绑定为一家人单元，与新郎分列两侧）；
- **仅三人约束**："Exactly three people … no fourth person, no extra hands or arms"
  （顺带治试拍复盘里"半个第四人/多一只手"）；
- **环境锚**：裸灯泡、雨打半地下气窗、齐沿红塑料桶（治背景漂成暖色餐厅）；
- 保留前轮微表情与去 `<d>` 成果，时间戳未动（时长/字幕轴不受影响）。

f3s02/03/04/08 `<d>` → 自然语言 `in Chinese`（沿用 f3s06 已验证句式）。

### 层间同步与口径修复
- 同步层：`manifest.json`（权威）→ `wf/`（384 草稿）→ `wf_one/`（一步直出）→
  `prompts/f3sXX.txt`（导出副本），全 8 镜 `wf == 前导段 + manifest` 校验 **PASS**。
- **口径归一**：前轮修复把 `<Audio 1>` 前导段写进了 f3s06 的 manifest；本轮统一为
  "manifest 不带前导段，wf 构建时拼接"（与 `build_stage1.py` 一致），避免重跑
  `build_stage1.py --all` 时出现双份前导段。

## 四、修复后审查（audit_prompts.py）

- 片3 `<d>` 标签：**0/8**；f3s06 微表情 7 处 / 情绪弧 7 处 = OK。
- f1/f2 `<d>`：16/16（未动，待确认后批量处理）。
- f3s03 残留 1 处静态词 "completely motionless"（描述背景中的新郎，风险低，记入下轮）。

## 五、待办

- [x] 重生成 f3s06（15:05 出片，`regen_f3s06/f3s06_修复重生成.mp4`，14:52 提交、含冷启 13 分钟）
- [ ] 片1/片2 16 镜 `<d>` 批量清除 + 微表情增强（**需确认**，同 f3s06/f3s02 句式）
- [ ] f3s03 静态词微调（可与上条合并做）

---

## 六、f3s06 重生成验证（2026-09-18 15:05）

成片：`_deliver/onestep_20260918/regen_f3s06/f3s06_修复重生成.mp4`
（远端 `f3_s06_00002_.mp4`；旧试拍版 `refined/f3s06.mp4` 与 gate 备份
`gates_one/f3s06.done.pilot1_bak` 均保留未动）

### 体检
362 帧 / 15.083s、768×1344、24fps、aac 2ch —— 与 manifest H3 口径一帧不差。

### 三个报告过的 bug 全部消失（抽帧 t1.0–t14.5）
| 旧病 | 旧证据 | 重生成 |
|---|---|---|
| 屏幕字幕 | v1 帧"是5二十万的替死鬼" | **7 个抽帧点均无字幕** |
| 人物关系错（新郎+丈母娘并肩、新娘消失） | `f3s06_v2_12.5.jpg` | 母女相邻为一家人单元，新郎独处近端持文件；新娘全程在画 |
| 第四只手/半个第四人 | 试拍复盘 | 恰好三人，无多余肢体 |
| 表情僵硬 | frozen in place | 新郎有前倾压文件/表情变化，母亲垂目、新娘侧首 |

### 残留偏差（如实记录）
- **对峙几何未完全成立**：设计的"90° 剖面、两阵营隔桌对峙"被 H3 画成
  **三人同排**（新郎最靠近镜头，新娘、丈母娘依次在后），全家面向同一侧。
  母女单元与新郎的区隔仍然清晰（中间隔着新娘+桌位），不再有"母子同框"误读，
  但"剑拔弩张的隔桌对峙"弱化为"一家三口听审"。竖幅三人正反打是 H3 构图
  弱项；若这是硬需求，换 seed 重试或改为过肩双人镜（裁掉第三人）。
- 新郎 7.5s 处表情偏"冷笑"幅度略大，10s 后回归冷峻，可接受。
- 道具文字仍为半可辨伪汉字（set dressing 固有冲突，与分辨率无关）。
